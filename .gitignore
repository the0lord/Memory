# secrets — never commit
.env
*.env
!.env.example

# learned AI state — machine-specific, regenerates each run
state.json

# local scratch solution store (the real shared store lives at SOLMEM_HOME)
solmem_store/

# python
__pycache__/
*.py[cod]
.venv/
venv/

# local-only docs and solmem — not tracked in git
README.md
BLEEDING_EDGE.md
SOLMEM.md

# local docs
DEPLOY.md


